# Web Server

## Konfigurasi Repo
 ```sh
   nano /etc/apt/sources.list
   ```

## Jalankan perintah
 ```sh
   apt update
   ```

## Install web server (apache)
 ```sh
   sudo apt install apache2 -y
   ```
